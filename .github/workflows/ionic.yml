name: Ionic CI
on:
  pull_request:
    paths:
      - 'angular-app/**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install
      - run: yarn start


